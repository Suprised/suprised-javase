package com.suprised.utils.impot.validator;

public class Validator {
    {
        // Importer importer = new Importer();
        // importer.parseData(); //解析数据
        // Validator validator = new Validator();验证数据
        // validator.build(new EmailRule()).build(new PhoneRule());
        // validator.validate();
        // validator.isSuccess(); 成功
        // 开始导入数据
        
        // 失败
        // validator.getErrorMessage(); 现实错误信息
    }
    
}